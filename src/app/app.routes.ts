import { Routes } from '@angular/router';
import { DashboardComponent } from './index/components/dashboard/dashboard';

export const routes: Routes = [
  {
    path: '',
    component: DashboardComponent,
  },
];
